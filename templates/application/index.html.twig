{% extends 'base.html.twig' %}

{% block title "subscribe" %}

{% block body %}
    {{ form_start(form) }}
        {{ form_widget(form) }}

        <input type="submit" class="btn btn-primary btn-sm shadow-none" value="{{ submitBtnText }}">
    {{ form_end(form) }}
{% endblock %}
